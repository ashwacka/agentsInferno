# Required for AI reasoners (derive_use_cases, recommend_adoption, evaluate_pipeline)
OPENAI_API_KEY=sk-...

# Optional: control plane URL when not using default
# AGENTFIELD_SERVER=http://localhost:8080

# Optional: model (LiteLLM format)
# OPENAI_MODEL=openai/gpt-4o
